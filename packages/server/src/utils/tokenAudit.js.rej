diff a/server/utils/tokenAudit.js b/server/utils/tokenAudit.js	(rejected hunks)
@@ -13 +13 @@ const q = queue({
-  concurrency: 1,
+  concurrency: 1
@@ -43 +43 @@ async function sendAdminAlert(user, diff) {
-      `,
+      `
@@ -57 +57 @@ async function userEarnings(user) {
-    $or: [{ status: 'paidout' }],
+    $or: [{ status: 'paidout' }]
@@ -68 +68 @@ async function userEarnings(user) {
-      .filter((e) => e.status === 'completed')
+      .filter(e => e.status === 'completed')
@@ -114 +114 @@ async function auditUser(handle) {
-    $or: [{ status: 'paidout' }, { status: 'expired' }],
+    $or: [{ status: 'paidout' }, { status: 'expired' }]
@@ -122 +122 @@ async function auditUser(handle) {
-  const postData = await earnings.map(async (e) => {
+  const postData = await earnings.map(async e => {
@@ -130 +130 @@ async function auditUser(handle) {
-      communityId: investment.communityId,
+      communityId: investment.communityId
@@ -183 +183 @@ async function listAllBalances() {
-  users.forEach((u) => console.log(u.handle, ',', u.balance));
+  users.forEach(u => console.log(u.handle, ',', u.balance));
